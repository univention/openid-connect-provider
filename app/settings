[oidc/konnectd/issuer_identifier]
Description = OpenID Connect Issuer Identifier. Setting this is mandatory and the setting must be a https URL which can be accessed by all applications and users. Adapting this value ONLY changes the configuration of the OpenID service. In addition, the apache2 configuration has to be adapted, the configuration for the provider is defined in /etc/apache2/conf-available/openid-connect-provider.conf
Description[de] = OpenID Connect Issuer Identifier. Diese Einstellung muss gesetzt sein, die hier konfiguriertre https URL muss von allen Applikationen und Benutzern erreichbar sein. Das Anpassen dieses Wertes ändert nur die Konfiguration des OpenID Dienstes. Zusätzlich muss die apache2 Konfiguration angepasst werden, die in  /etc/apache2/conf-available/openid-connect-provider.conf definiert ist.
Type = String
Required = false
Show = Settings
Scope = outside

[oidc/konnectd/allow_dynamic_client_registration]
Description = Allow dynamic client registration. When activated, clients can register themselves and make authorized calls to the token endpoint.
Description[de] = Dynamische Clientregistrierung erlauben. Bei Aktivierung können sich Clients selbst anmelden und Aufrufe gegen den Tokenendpunkt durchführen.
Type = Bool
InitialValue = false
Required = false
Show = Settings
Scope = outside
